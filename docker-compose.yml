version: '3.8'
services:
  cryptobot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./config_backups:/app/config_backups
    ports:
      - "8501:8501"
